import test from 'node:test';import assert from 'node:assert/strict';import {OfflineAudioContext} from 'node-web-audio-api';import {JetLayers} from '../src/audio/JetLayers.js';
async function render(params){const ctx=new OfflineAudioContext(1,44100,44100),jet=new JetLayers(ctx,ctx.destination);jet.configure(params);jet.start();jet.update({rpm:7000,rpmNorm:.95,throttle:1});const b=await ctx.startRendering();let energy=0;for(const v of b.getChannelData(0)){assert.ok(Number.isFinite(v)&&Math.abs(v)<1);energy+=v*v;}jet.dispose();assert.equal(jet.nodes.length,0);return energy;}
test('spool and thrust buses can be enabled independently and both disabled',async()=>{assert.equal(await render({jetSpoolEnabled:0,jetThrusterEnabled:0,jetAfterburnerEnabled:0}),0);assert.ok(await render({jetThrusterEnabled:0,jetAfterburnerEnabled:0})>0);assert.ok(await render({jetSpoolEnabled:0,jetAfterburnerEnabled:0})>0);});
