import test from 'node:test';import assert from 'node:assert/strict';import {parseDestination,crosses88} from '../src/themes/timeCircuitModel.ts';
test('time circuit keypad validates real calendar dates and leap years',()=>{assert.ok(parseDestination('022920241230'));assert.equal(parseDestination('022920231230'),null);assert.equal(parseDestination('133120241230'),null);assert.equal(parseDestination('123120242460'),null);assert.equal(parseDestination('12312024'),null);assert.equal(parseDestination('010100010000')?.getFullYear(),1);});
test('temporal threshold is 88 mph regardless of display units and only on rising crossing',()=>{assert.equal(crosses88(87*.44704,88*.44704),true);assert.equal(crosses88(88*.44704,90*.44704),false);assert.equal(crosses88(90*.44704,87*.44704),false);assert.equal(crosses88(39,141.622272/3.6),true);});
